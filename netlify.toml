[build]
  publish = "public"
  functions = "netlify/functions"

# Redirige /api/* a las funciones serverless (queda más limpio en el frontend)
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
